import { StandUpDirective } from './stand-up.directive';

describe('StandUpDirective', () => {
  it('should create an instance', () => {
    const directive = new StandUpDirective();
    expect(directive).toBeTruthy();
  });
});
