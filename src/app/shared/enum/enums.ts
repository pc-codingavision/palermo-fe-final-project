export enum Category {
  SingleRoom = 'singleroom',
  Apartment = 'apartment',
  None = 'none',
}

export enum PhoneType {
  None = 'none',
  Mobile = 'mobile',
  Home = 'home',
  Work = 'work',
}

export enum Role {
  None = 'none',
  Manager = 'manager',
  Landlord = 'landlord',
  Tenant = 'tenant',
}

export enum Status {
  Open = 'active',
  Closed = 'inactive',
}

export enum Toilet {
  Inside = 'inside',
  Outside = 'outside',
  Shared = 'shared',
  None = 'none',
}

export enum Icon {
  Heart = 'heart',
  CheckBox = 'checkbox',
  Radio = 'radio',
  Star = 'star',
  Circle = 'circle',
}
